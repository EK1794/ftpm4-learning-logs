# 令和5年度 春期 基本情報技術者試験（科目A）
- **出典**: [過去問道場](https://www.fe-siken.com/kakomon/05_haru/)

## 💡 概念・計算
### 10進数と16進数の変換
* `XY.Z` の場合
  * `X`: 16^1の桁
  * `Y`: 16^0の桁
  * `Z`: 16^-1の桁
* **Tips**: `0～F`を`0～16`に変換し、桁を掛ける。（例: 16進数の `0.C` → 12/16 = 3/4 = `0.75`）

### メモリ・ポインタ
* **ポインタ**: 前後のアドレスを指定する。
  * （例）元の表が `A → K → T` の場合、Aの次ポインタとTの前ポインタは同じ「Kのアドレス」になる。間にGを挟んで `A → G → K → T` にする場合、Aの次とKの前にGのアドレスが入る。
* **メモリインターリーブ**: ブロック分けされたメモリ（メモリバンク）の複数へ同時並行的にアクセスし、データ転送速度を向上させる高速化技法。

## 🌐 ネットワーク・通信
* **DHCP**: TCP/IPネットワークで、接続するノードへIPアドレス等を自動割り当てするプロトコル。
* **BOOTP**: DHCPの前身。現在はほぼ使われない。
* **SNMP**: TCP/IPネットワーク環境（ルータやサーバ等）の監視・管理プロトコル。
  * **MIB**: SNMPが参照するために、ネットワーク機器が外部へ公開している自身の状態情報。
* **ICMP**: ネットワーク通信の問題を診断するためのネットワーク層プロトコル。
  * **ping**: ICMPの `echo request` を投げ、`echo reply` が返るかで対象との通信可否を確認するコマンド。

## 🖥️ CG・アーキテクチャ・その他
* **エッジコンピューティング**: 処理をエッジ（デバイスやユーザーに近い場所）で実行する。対義語はクラウドコンピューティング（データセンターで実行）。
* **CG処理**:
  * **レンダリング**: CG映像作成における最終段階として、物体のデータをディスプレイに描画できるように映像化する処理。
  * **クリッピング**: 最終的な表示画面の範囲外（不可視部分）をCGの描画対象から除外する処理。
  * **アンチエイリアシング**: 画素数が有限であるために図形の境界に生じる階段状のギザギザを目立たなくする処理。
  * **シェーディング**: 物体の表面に陰影を付け、立体感を生じさせる処理。
* **ACID特性 (トランザクション)**:
  * **A (Atomicity: 原子性)**: トランザクション内の処理が全て実行されるか、全く実行されないかを保証。
  * **C (Consistency: 一貫性)**: データの矛盾が生じない（整合性）ことを保証。
  * **I (Isolation: 独立性/隔離性)**: 同時実行と順次実行の結果が等しくなることを保証。
  * **D (Durability: 耐久性/永続性)**: 正常終了後の結果は、障害が発生しても失われないことを保証。